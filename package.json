{
  "name": "housekenya-deployment",
  "private": true,
  "scripts": {
    "install:fronted": "npm --prefix fronted install",
    "install:backend": "npm --prefix backend install",
    "install:all": "npm run install:fronted && npm run install:backend",
    "build": "npm --prefix fronted run build"
  }
}
